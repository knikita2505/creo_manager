# Creo Manager

Платформа для управления креативами: пакетная загрузка видео на YouTube, мониторинг банов в Google Ads, генерация модальных видео-креативов.

## Структура проекта

```
creo_manager/
├── backend/          # FastAPI backend
├── frontend/         # Next.js frontend
└── Project description/  # Документация проекта
```

## Быстрый старт

### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл и укажите настройки БД и API ключи
```

5. Настройте Supabase:
   - У вас уже есть проект в Supabase
   - Получите строку подключения (Settings → Database → Connection string → URI)
   - Добавьте в `.env` как `DATABASE_URL` (формат: `postgresql+asyncpg://...`)
   - Проверьте подключение: `python scripts/check_db.py`
   - Таблицы создадутся автоматически при первом запуске
   
   📖 Подробная инструкция: [docs/supabase-setup.md](docs/supabase-setup.md)  
   🚀 Быстрый старт: [docs/quick-start.md](docs/quick-start.md)

6. Убедитесь, что установлен FFmpeg:
```bash
ffmpeg -version
```

7. Проверьте конфигурацию .env:
```bash
cd backend
source venv/bin/activate
python scripts/check_env.py
```

8. Проверьте подключение к Supabase:
```bash
python scripts/check_db.py
```

9. Запустите сервер:

**Вариант 1: Использование скрипта (рекомендуется)**
```bash
./start_backend.sh
```

**Вариант 2: Вручную**
```bash
cd backend
source venv/bin/activate
python run.py
```

### Frontend

**Вариант 1: Использование скрипта (рекомендуется)**
```bash
./start_frontend.sh
```

**Вариант 2: Вручную**
```bash
cd frontend
npm install
npm run dev
```

Приложение будет доступно по адресу http://localhost:3000

### Запуск всего проекта одновременно

Для одновременного запуска backend и frontend:
```bash
./start.sh
```

Это запустит оба сервера в отдельных окнах терминала. Для остановки закройте соответствующие окна.

📖 Подробнее о скриптах: [SCRIPTS.md](SCRIPTS.md)

## Конфигурация

### Backend (.env)

- `DATABASE_URL` - строка подключения к PostgreSQL
- `SECRET_KEY` - секретный ключ для JWT
- `YOUTUBE_CLIENT_ID`, `YOUTUBE_CLIENT_SECRET` - OAuth credentials для YouTube
- `STORAGE_PATH` - путь для хранения видео файлов
- `MAX_PARALLEL_UPLOADS` - максимальное количество параллельных загрузок

### Frontend

Создайте файл `.env.local`:
```
NEXT_PUBLIC_API_URL=http://localhost:8000
```

## Функционал MVP

- ✅ Загрузка видео файлов
- ✅ Очистка метаданных
- ✅ Уникализация видео (изменение длительности, размера, FPS, битрейта)
- ✅ Генерация дополнительных ориентаций (square, portrait, landscape)
- ✅ Загрузка на YouTube со статусом unlisted
- ✅ Таблица результатов загрузок
- ✅ Повторная попытка загрузки при ошибках

## Технологии

### Backend
- FastAPI
- SQLAlchemy 2.0 (async)
- PostgreSQL
- FFmpeg
- Google API Client

### Frontend
- Next.js 14
- React 18
- TypeScript
- Tailwind CSS
- React Query
- Lucide Icons

## Следующие шаги

1. Настройка интеграций (YouTube, Google Drive, Google Ads, Telegram)
2. Мониторинг банов в Google Ads
3. Генерация модальных видео-креативов

