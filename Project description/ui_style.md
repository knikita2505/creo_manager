# UI / UX Style Guide

## Общие принципы

- **Минимализм и воздух**  
  Интерфейс должен быть легким, чистым, без визуального шума. Используем максимум пространства, делаем элементы читаемыми и визуально отделёнными.

- **Референсы**  
  Ориентируемся на стилистику:
  - Notion — за счет минимализма, легкости, белого пространства
  - Linear — за счет анимаций, карточек, modern-B2B energy
  - macOS — за счёт гладкости, плавных теней, выверенной типографики

- **Mobile-first**  
  Все компоненты должны быть адаптивны. Интерфейс должен корректно выглядеть и на десктопе, и на смартфоне.

---

## Цвета

- **Фон**: `#ffffff` (светлый режим), `#0f0f0f` (тёмный режим — опционально)
- **Основной текст**: `#1a1a1a`
- **Вторичный текст**: `#6b7280`
- **Акценты**: синий `#3b82f6` или фиолетовый `#8b5cf6`
- **Ошибки**: красный `#ef4444`
- **Успех**: зелёный `#10b981`
- **Фон карточек**: `#f9fafb`

(Можно использовать Tailwind palette: `gray-900`, `gray-100`, `blue-500`, `red-500`, `green-500`)

---

## Шрифты и типографика

- **Базовый шрифт**: `Inter`, `SF Pro`, `system-ui`, `sans-serif`
- **Размеры**:
  - Заголовок h1: `text-2xl font-semibold`
  - Заголовок h2: `text-xl font-medium`
  - Текст: `text-base`
  - Вторичный текст: `text-sm text-gray-500`
- **Интерлиньяж**: `leading-relaxed`
- **Отступы между секциями**: `mb-6` / `mt-6`

---

## Компоненты

### 🔘 Кнопки

- **Стиль**: закругленные, `rounded-2xl`, padding `px-4 py-2`
- **Primary**: синий/фиолетовый фон, белый текст
- **Secondary**: серый фон `bg-gray-100`, чёрный текст
- **States**: `hover`, `active`, `focus`, `disabled`
- **Иконки**: слева от текста, с отступом `mr-2`

### 📄 Таблицы

- **Header**: `bg-gray-50`, `text-xs uppercase`, `tracking-wide`
- **Строки**: чередование цвета (optional), `hover:bg-gray-50`
- **Фильтры**: сверху, в виде дропдаунов или input
- **Пагинация**: внизу, компактная, с возможностью смены размера таблицы

### 🧩 Карточки / контейнеры

- `rounded-xl`, `shadow-md`, `bg-white`, `px-5 py-4`
- Подписи к разделам — в виде `text-sm text-gray-500 uppercase tracking-wider`

### 📤 Drag & Drop загрузка

- Область выделена пунктирной рамкой `border-dashed`, подсвечивается при наведении
- Иконка + надпись "Перетащите сюда видео или выберите файл"
- После загрузки — прогрессбар и статус по каждому файлу

---

## Анимации и поведение

- **Загрузки**: использовать `skeleton` или `shimmer`-эффекты
- **Модальные окна**: открываются с fade-in и scale
- **Уведомления (toasts)**: снизу справа, с auto-dismiss через 3–4 сек
- **Ошибки**: подсвечиваем поля `border-red-500`, добавляем текст ошибки
- **Кнопка "Сохранить"** — появляется при изменениях, неактивна по умолчанию

---

## Иконки и иллюстрации

- Используем `Lucide Icons` (от создателей Feather)
- Размер: `20x20` в кнопках, `24x24` в карточках
- Все иконки должны быть в едином стиле (тонкие линии, монохром)

---

## Состояния интерфейса

- **Loading**: spinners, skeletons
- **Success**: зелёные чекмарки, тост с "Готово"
- **Error**: красный тост или баннер с пояснением
- **Empty state**: иконка + заголовок + текст "Вы пока не загрузили ни одного видео"

---

## Тематические секции

### Загрузки

- Карточки с превью/названием, сгруппированы по исходнику
- Группа с toggle-accordion на версии (square/landscape/portrait)
- Кнопка "Загрузить ещё" сверху

### Интеграции

- Список с логотипами (YouTube, Drive, Google Ads, Telegram)
- Кнопка `Подключить`, статус: "Подключено / Неактивно"
- Возможность переподключить

### Мониторинг

- Таблица со статусами по видео (Approved / Limited / Not Eligible)
- Фильтры по кампаниям, дате, статусу
- Лента уведомлений с отметкой времени

### Конструктор модалок

- Визуальный редактор шаблона (inputs для текста, селект темы)
- Выбор фона (upload 9 картинок)
- Предпросмотр модалки
- Кнопка `Сгенерировать видео`

---

## Технические библиотеки

- UI-компоненты: `shadcn/ui` (обёртка над Radix + Tailwind)
- Иконки: `lucide-react`
- Анимации: `framer-motion`
- Таблицы и формы: `react-hook-form`, `@tanstack/react-table`

