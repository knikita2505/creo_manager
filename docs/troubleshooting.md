# Решение проблем

## Ошибка подключения к БД

### Ошибка: `ModuleNotFoundError: No module named 'psycopg2'` или проблемы с подключением при использовании `+asyncpg`

Эта ошибка возникает, когда:
- SQLAlchemy пытается использовать синхронный драйвер `psycopg2` (без `+asyncpg`)
- Или при использовании `+asyncpg` с прямым подключением (порт 5432) возникает ошибка подключения

**Решение:**

📖 **Полная инструкция:** [asyncpg-supabase-fix.md](asyncpg-supabase-fix.md)

**Краткое решение:**

1. **Для asyncpg используйте Connection Pooler (порт 6543), а не прямое подключение (порт 5432):**

   - Откройте Supabase Dashboard → Settings → Database → Connection string
   - Выберите **"Session mode"** (Connection Pooler)
   - Скопируйте URL - он будет содержать `pooler.supabase.com:6543`
   - Формат: `postgresql+asyncpg://postgres.[REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres`

2. **Пример правильного URL:**
   ```
   postgresql+asyncpg://postgres.blnoiaxjswehjulocafl:password@aws-0-us-east-1.pooler.supabase.com:6543/postgres
   ```

3. **Обновите `.env` файл с Connection Pooler URL**

4. **Проверьте подключение:**
   ```bash
   cd backend
   source venv/bin/activate
   python scripts/check_db.py
   ```

### Ошибка: `nodename nor servname provided, or not known`

Это означает, что приложение не может найти хост базы данных.

**Решение:**

1. **Проверьте формат DATABASE_URL в `.env`**

   Правильный формат для Supabase:
   ```
   postgresql+asyncpg://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
   ```

2. **Получите правильную строку подключения:**

   - Откройте Supabase Dashboard
   - Перейдите в **Settings** → **Database**
   - Найдите секцию **Connection string**
   - Выберите вкладку **URI**
   - Скопируйте строку подключения

3. **Проверьте, что проект Supabase активен:**

   - Убедитесь, что проект не в паузе
   - Если проект приостановлен, разбудите его в Dashboard

4. **Проверьте правильность замены:**

   - `[PASSWORD]` → ваш пароль БД (можно сбросить в Settings → Database)
   - `[PROJECT-REF]` → идентификатор проекта (находится в URL проекта)

5. **Используйте правильный формат для asyncpg:**

   - Для asyncpg нужен формат: `postgresql+asyncpg://...`
   - Если в Supabase указан формат `postgresql://`, добавьте `+asyncpg`:
     ```
     postgresql://... → postgresql+asyncpg://...
     ```
   - **Примечание:** Приложение автоматически исправит формат, но лучше использовать правильный формат с самого начала

### Проверка конфигурации

```bash
cd backend
source venv/bin/activate
python scripts/check_env.py
```

### Проверка подключения

```bash
python scripts/check_db.py
```

## Другие распространенные ошибки

### Ошибка: `ModuleNotFoundError`

**Решение:**
```bash
cd backend
source venv/bin/activate
pip install -r requirements.txt
```

### Ошибка: `Permission denied` при запуске скриптов

**Решение:**
```bash
chmod +x start_backend.sh start_frontend.sh start.sh
```

### Ошибка: FFmpeg не найден

**Решение:**

macOS:
```bash
brew install ffmpeg
```

Linux:
```bash
sudo apt-get install ffmpeg
```

### Ошибка синтаксиса в Python

Убедитесь, что используете правильную версию Python (3.11+):
```bash
python3 --version
```

Если версия ниже, установите Python 3.11+ или используйте виртуальное окружение с правильной версией.

