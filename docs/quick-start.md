# Быстрый старт с Supabase

## Шаг 1: Получение строки подключения из Supabase

1. Откройте ваш проект в [Supabase Dashboard](https://app.supabase.com)
2. Перейдите в **Settings** → **Database**
3. Найдите секцию **Connection string**
4. Выберите вкладку **URI**
5. Скопируйте строку подключения

**Важно:** Замените `[YOUR-PASSWORD]` на ваш пароль БД (или используйте `postgres` если не меняли)

Пример строки:
```
postgresql://postgres:[YOUR-PASSWORD]@db.abcdefghijklmnop.supabase.co:5432/postgres
```

Для использования с asyncpg добавьте `+asyncpg`:
```
postgresql+asyncpg://postgres:[YOUR-PASSWORD]@db.abcdefghijklmnop.supabase.co:5432/postgres
```

## Шаг 2: Настройка .env файла

1. Перейдите в директорию `backend`
2. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

3. Откройте `.env` и вставьте строку подключения:
```env
DATABASE_URL=postgresql+asyncpg://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
```

## Шаг 3: Проверка подключения

Запустите скрипт проверки:
```bash
cd backend
python scripts/check_db.py
```

Скрипт проверит подключение и предложит создать таблицы.

## Шаг 4: Запуск приложения

После успешной проверки подключения запустите сервер:
```bash
python run.py
```

При первом запуске таблицы создадутся автоматически.

## Решение проблем

### Ошибка подключения

**Проблема:** `Connection refused` или `authentication failed`

**Решение:**
1. Проверьте правильность пароля в строке подключения
2. Убедитесь, что используете правильный формат: `postgresql+asyncpg://...`
3. Проверьте, что проект Supabase активен (не в паузе)

### Ошибка создания таблиц

**Проблема:** `relation already exists` или `permission denied`

**Решение:**
1. Если таблицы уже существуют - это нормально, они будут переиспользованы
2. Проверьте права доступа к БД в Supabase Dashboard

### Медленное подключение

**Решение:** Используйте Connection Pooler вместо прямого подключения:
```
postgresql+asyncpg://postgres.[REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

Найдите Connection Pooler строку в Supabase Dashboard → Settings → Database → Connection string → Session mode

## Готово! 🎉

Теперь ваше приложение подключено к Supabase и готово к работе.

